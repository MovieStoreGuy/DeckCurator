---
language: cpp

addons:
    apt:
        sources:
            - ubuntu-toolchain-r-test
            - deadsnakes
        packages:
            - g++-4.8
            - python3.5
            - python3.5-dev

install:
    - pip install pybind11 pytest

script:
    - python setup.py build_ext
